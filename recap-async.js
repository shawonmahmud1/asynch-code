/* 
1. find user bt username
2. find post by userId
3. find last post
4. find comment by post id
5. find  latest comment
6. find username who comment by latest

*/
/* 
1. user? username = [username];
2. post? useId = [userId]
3. comments? postId = [postId]
4. user? username = [username]
*/

function get(path, cb) {
  const data = {};
  cb(data);
}

function getUserNameFromComment(username) {}
getUserNameFromComment("Shawon");
